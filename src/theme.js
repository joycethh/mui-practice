import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#2196f3",
      light: "#e3f2fd",
    },
    secondary: {
      main: "#e57373",
      light: "#ffebee",
    },
  },
});
