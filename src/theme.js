import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#009688",
      light: "#e0f2f1",
    },
    secondary: {
      main: "#e57373",
      light: "#ffebee",
    },
  },
});
